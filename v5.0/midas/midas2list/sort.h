#ifndef SORT_H
#define SORT_H

#include <time.h>

#include "sort_and_assemble_list.h"
#include "map.h"

FILE* out;

int min_TSUP, max_TSUP, minmax_DROPPED, depth_DROPPED, pileup_DROPPED, total_FRAGMENTS;
int analyze_fragment(Grif_event*, short*){return -1;};
#endif
